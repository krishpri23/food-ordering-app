import React from "react";
import ReactDOM from "react-dom/client";

const heading = <h2 className="heading"> Hello ! </h2>;

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);
